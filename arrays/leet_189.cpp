#include<iostream>
using namespace std;
int main() {
    int k;
    cout<<"Enter the number :";
    cin>>k;
    int arr[] ={1,2,3,4,5,6,7};
    int n = sizeof(arr)/sizeof(arr[0]);
    num = n-k;
    int temp[n];
    for (int i = 0; i<=num; i++)
    {
        temp[i] = arr.pop_back(i); 

for (int i = 0; i < k; i++)
{
    
}
    }
    
    return 0;
}