#include<iostream>
#include<vector>
using namespace std;

int main() {
    vector<int> arr = {3, 0, 1};
    int n = arr.size();
    // int total = n * (n + 1) / 2; // Sum of 0 to n
    // int sum = 0;
    // for (int num : arr) {
    //     sum += num;
    // }
    // cout << (total - sum) << endl; // The missing number
    // return 0;
    
}